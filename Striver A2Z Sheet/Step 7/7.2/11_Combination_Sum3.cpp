#include <bits/stdc++.h>
using namespace std;

void comboSum(vector<int> candidates, vector<int> arr, vector<vector<int>> &ans, int n)
{
}
vector<vector<int>> combinationSum3(int k, int n)
{
    vector<int> arr;
    vector<int> candidates = {1, 2, 3, 4, 5, 6, 7, 8, 9};
    vector<vector<int>> ans;
    comboSum(candidates, arr, )
}

int main()
{

    return 0;
}